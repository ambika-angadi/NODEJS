// Function to calculate the area of a circle
const calculateArea = (radius) => {
    return Math.PI * Math.pow(radius, 2);
  };
  
  // Exporting the calculateArea function
  module.exports = calculateArea;
  